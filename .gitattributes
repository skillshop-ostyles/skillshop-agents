# Consistent line endings - eliminates CRLF warning flood on every commit.
* text=auto eol=lf

# PowerShell scripts keep CRLF (Windows tooling expects it).
*.ps1 text eol=crlf

# Don't touch binary files.
*.db binary
*.png binary
*.jpg binary
*.gif binary