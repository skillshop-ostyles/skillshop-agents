# doku-drift-fixture

Kurze Testfixture fuer den doku-drift-detektor.

## Build

Die Hauptlogik lebt in `src/greet.ts` und exportiert `formatGreeting`.

Fuer den Build:

```bash
npm run deploy
```

Ein alter Pfad, der nicht mehr existiert: `src/missing.ts`.
