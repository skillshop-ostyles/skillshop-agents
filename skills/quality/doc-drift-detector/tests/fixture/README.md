# doc-drift-fixture

Short test fixture for the doc-drift-detector.

## Build

The main logic lives in `src/greet.ts` and exports `formatGreeting`.

For the build:

```bash
npm run deploy
```

An old path that no longer exists: `src/missing.ts`.
