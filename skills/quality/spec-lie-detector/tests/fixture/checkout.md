# Checkout Flow

The checkout process must be fast and user-friendly.

A discount code can only be applied ONCE per order - combining multiple
discount codes is not supported.

If payment fails, the system shows an error message and the cart
is preserved for the next attempt.

After successful payment, an order confirmation is sent via email.
