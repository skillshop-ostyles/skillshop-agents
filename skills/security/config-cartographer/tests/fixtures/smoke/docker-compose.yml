version: '3.8'
services:
  app:
    image: myapp:latest
    environment:
      - DATABASE_URL
      - API_KEY=${API_KEY}
  worker:
    image: myworker:latest
    environment:
      WORKER_CONCURRENCY: 4
      REDIS_URL: redis://localhost:6379
